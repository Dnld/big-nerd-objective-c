#include <stdio.h>

int main(int argc, const char * argv[])
{
    unsigned int x = 255;
    printf("x is %d.\n", x);
    printf("In octal, x is %o.\n", x);
    printf("In hexadecimal, x is %x.\n", x);
    
    return 0;
}